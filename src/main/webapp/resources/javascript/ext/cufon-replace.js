function loadCufon(){
/*Cufon.replace('h2, h3, h4, h5, h6', { hover: 'true' } );*/
/*Cufon.replace('.menu > ul > li', { hover: true, hoverables: { li: true }, ignore: { ul: true } } );*/
/*Cufon.replace('.sexyslider-title a', { textShadow: '#000 0 1px', hover: 'true' } );
Cufon.replace('.homepage-title p', { textShadow: '#000 0 1px' } );
Cufon.replace('.price', { textShadow: '#246500 0 1px' } );*/
/*Cufon.replace('.main-title span', { textShadow: '#f3f3f3 0 1px' } );*/
//Cufon.replace('.btn-1', { textShadow: '#fff 0 1px', hover: 'true' } );
//Cufon.replace('.btn-2', { textShadow: '#fff 0 1px', hover: 'true' } );
//Cufon.replace('.btn-3', { textShadow: 'none', hover: 'true' } );
//Cufon.replace('.btn-4', { textShadow: 'none', hover: 'true' } );
/*Cufon.replace('.homepage-columns-item .title', { textShadow: '#47250f 0 1px' } );
Cufon.replace('.homepage-columns-item-2 .title', { textShadow: '#47250f 0 1px' } );
Cufon.replace('.date .day', { hover: 'true' } );
Cufon.replace('.pages a', { hover: 'true' } );
Cufon.replace('.tabs-1 a', { hover: 'true' } );
Cufon.replace('blockquote', { hover: 'true' } );
Cufon.replace('.comments-item .user', { hover: 'true' } );
Cufon.replace('.no-comments p', { hover: 'true' } );
Cufon.replace('.no-logo p', { textShadow: '#4b4b4b 0 1px' } );
Cufon.replace('.navigation .nr', { hover: 'true' } );
Cufon.replace('.menu-card .item .title', { hover: 'true' } );
Cufon.replace('.menu-card .item .menu-price', { hover: 'true' } );
Cufon.replace('.success p', { hover: 'true' } );
Cufon.replace('.search-wrapper p', { hover: 'true' } );
Cufon.replace('.results', { hover: 'true' } );
Cufon.replace('.no-results p', { hover: 'true' } );*/
}